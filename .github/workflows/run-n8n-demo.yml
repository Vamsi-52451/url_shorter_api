name: n8n Demo CI

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Repo Checkout
        uses: actions/checkout@v3

      - name: Print Hello
        run: echo "This workflow triggered from n8n 🚀"
